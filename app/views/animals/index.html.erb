<% content_for(:title, "Animals | Wildlife-Tracker") %>

<h1>Animals</h1>

<ul>
  <% @animals.each do |animal| %>
    <li><%= link_to animal.species, animal_path(animal) %></li>
  <% end %>
</ul>

<hr>

<%= link_to "New Critter", new_animal_path %>
